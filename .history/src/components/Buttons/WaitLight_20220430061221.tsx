import React from 'react'
import styles from "./Button.module.css"

const Wait = () => {
  return (
    <div className={`${styles.light} capitalizwe`}>Join the waitlist</div>

  )
}

export default Wait