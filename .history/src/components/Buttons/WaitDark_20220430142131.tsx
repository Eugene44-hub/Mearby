import React from 'react'
import styles from "./Button.module.css"

const Wait = () => {
  return (
    <button className={`${styles.dark} text-white`}>join the waitlist</button>
  )
}

export default Wait