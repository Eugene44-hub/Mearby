import React from 'react'
interface Props {
  color:string;
  width:string;
  height:string;
text:string;
}
const Button = (props.str) => {
  return (
    <div style={width}></div>
  )
}

export default Button