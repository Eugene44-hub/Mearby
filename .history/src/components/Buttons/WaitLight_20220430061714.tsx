import React from 'react'
import styles from "./Button.module.css"

const Wait = () => {
  return (
    <buttonclassName={`${styles.light} capitalize`}>Join the waitlist</buttonclassName=>

  )
}

export default Wait