import React from 'react'
import styles from "./Button.module.css"

const Wait = () => {
  return (
    <button className={`${styles.light} capitalize`}>Join the waitlist</butto>

  )
}

export default Wait